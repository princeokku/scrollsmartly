(function(){
  // Setup scrollsmartly
  smartly.set({
    position: 'center',
    easing: 5
  });

  smartly.all();
  
}());

$(function(){
  /*
  $('#intro').css(
    'width',
    $(window).width() + 'px'
  );
  */
});
